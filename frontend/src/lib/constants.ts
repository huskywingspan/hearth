/** Default message TTL in milliseconds (5 minutes). */
export const DEFAULT_TTL_MS = 5 * 60 * 1000;

/** Heartbeat interval in milliseconds (30 seconds). */
export const HEARTBEAT_INTERVAL_MS = 30_000;

/** Presence poll interval in milliseconds (15 seconds). */
export const PRESENCE_POLL_INTERVAL_MS = 15_000;

/** Time sync interval in milliseconds (5 minutes). */
export const TIME_SYNC_INTERVAL_MS = 5 * 60 * 1000;

/** Token auto-refresh interval in milliseconds (10 minutes). */
export const TOKEN_REFRESH_INTERVAL_MS = 10 * 60 * 1000;

/** Max messages to fetch at once. */
export const MESSAGE_PAGE_SIZE = 200;

/** Typing indicator debounce in milliseconds. */
export const TYPING_DEBOUNCE_MS = 3000;
