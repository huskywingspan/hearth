# Hearth — Environment Configuration
# Copy to .env and fill in your values
#
# Quick start:
#   cp .env.example .env
#   # Edit .env with your domain and secrets
#   docker compose up -d

# ================================================
# Domain (no protocol prefix, no trailing slash)
# ================================================
HEARTH_DOMAIN=hearth.example

# ================================================
# PocketBase
# ================================================
# 32-byte hex string for encrypting settings at rest
# Generate: openssl rand -hex 32
PB_ENCRYPTION_KEY=

# ================================================
# LiveKit
# ================================================
LIVEKIT_API_KEY=hearth-api-key
# Generate: openssl rand -hex 32
LIVEKIT_API_SECRET=

# ================================================
# HMAC Invite Tokens (Stateless)
# ================================================
# Current signing key. Generate: openssl rand -hex 32
HMAC_SECRET_CURRENT=
# Previous key — kept for rotation grace period. Leave empty initially.
HMAC_SECRET_OLD=

# ================================================
# Proof of Work
# ================================================
# Leading zero bits for SHA256 partial collision
# 20 ≈ 1-2 seconds on modern hardware. Lower = easier, higher = harder.
POW_DIFFICULTY=20
